package com.placeballspring;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * PlaceBall Spring Boot Application
 * 야구장 구역 점령 대시보드 - Spring Boot 변환 버전
 *
 * 원본: React + Express (TypeScript) → Spring Boot Java 변환
 */
@SpringBootApplication
public class PlaceballspringApplication {

    public static void main(String[] args) {
        SpringApplication.run(PlaceballspringApplication.class, args);
    }
}
