/**
 * main.tsx - React 앱 진입점
 *
 * 원본: baseballai-bicevcq9/client/src/main.tsx (동일)
 */
import { createRoot } from "react-dom/client";
import App from "./App";
import "./index.css";

createRoot(document.getElementById("root")!).render(<App />);
